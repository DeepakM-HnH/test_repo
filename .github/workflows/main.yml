name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Display Start Message new26
        run: echo "Starting the build process..."
        
      - name: Build Project
        run: dotnet build
