name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Display Start Message New14
        run: echo "Starting the build process..."

      - name: Change to Project Directory
        run: cd TestBuildApp

      - name: Build Project
        run: dotnet build
